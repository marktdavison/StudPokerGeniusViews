//
//  Main.swift
//  DeckOfCards
//
//  Created by Mark Davison on 29/05/2017.
//  Copyright © 2017 lifeline. All rights reserved.
//
// Card shuffling and dealing.
import UIKit
/*
public func shuffleAndDeal () {

    var myDeckOfCards = DeckOfCards()
    myDeckOfCards.shuffle() // place Cards in random order

    // deal all 52 Cards with for...in
    for i in 1 ... myDeckOfCards.numberOfCards {
        
        if let card = myDeckOfCards.dealCard() { // deal and unwrap Card
            
            // determine which player gets the card
            if i % 4 = 1 {
                p1Cards.text = card.description
            }
            
            print(card.description, "\n") // display Card

        }
    }

    myDeckOfCards.shuffle()

    // deal all 52 Cards with while
    var i = 0

    while let card = myDeckOfCards.dealCard() { // deal and unwrap Card
        i += 1
        while let card = myDeckOfCards.dealCard() { // deal and unwrap Card
            i += 1
            print(card.description) // display Card
            
        }
    
    }
} */


